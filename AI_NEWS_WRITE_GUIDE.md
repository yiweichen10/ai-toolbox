# AI 快讯内容规范（简短化）— 参考副本

> **状态（08-16）**：以下规范**已直接写入 automation-1784555527714 的步骤 1.5**（自动化在当前账号，无需手动粘贴）。本文件仅作参考与留档。
>
> 定位：**快讯 = 每天最简短的热点**。数据源是 aihot API 最新报道（fetch_aihot_news.py），改写只做"最小简短化"，不做长提炼、不做库存治理。

---

### 步骤1.5：快讯简短化（Agent 亲自执行，不调 API）

打开 `data/news_当天.json`，逐条精简，**保持 JSON 合法**（改完用 `json.loads` 自校验通过再继续）。

1. **title**：中文、≤30 字、结尾**无标点**、简短直接（如"SpaceX 完成收购 Cursor"）；英文源标题译中文，产品名/模型名（GPT-5.6、Cursor）保留
2. **summary**：≤50 字一句话抓要点（保留数字/结果），**禁空话词**（建议关注/建议XX/可关注/推荐关注/感兴趣可/值得一试/欢迎/敬请期待），禁复述标题，**禁编造**
3. 中英文/数字与中文之间加空格（禁"Cursor并入SpaceX"式粘连；"38.3倍"数字+单位属正常写法）
4. 同日同一事件多条 → 只留一条（优先带金额/来源权威的），删后重排 id（当天日期-001 起）
5. 字段 category/source/source_url/published_at 一律不动；条数只减不增

### 红线
- 数据必须来自 aihot API 当天最新报道，改写出处不换、事实不编。
- 快讯不是文章：摘要一句话扫完即懂，绝不展开成段落。
- 门禁 `check_news_quality.py` 在 build 时自动跑（只报告不阻断）：硬伤（空摘要/中英文粘连等）会打印警告，按提示修复当天文件即可。
