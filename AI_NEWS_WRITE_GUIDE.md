# AI 快讯内容规范（简短化 + 原创评注）— 参考副本

> **状态（2026-08-25 更新）**：以下规范已直接写入 automation-1784555527714 的步骤1.5（自动化在当前账号，无需手动粘贴）。本文件仅作参考与留档。
>
> 定位：**快讯 = 每天最简短的热点**。数据源是 aihot API 最新报道（fetch_aihot_news.py），改写做"最小简短化 + 原创评注"（2026-08-25 起每条加 commentary 字段，合规去新闻化：转载 → 编辑整理+评注）。

---

### 步骤1.5：快讯简短化 + 评注（Agent 亲自执行，不调 API）

打开 `data/news_当天.json`，逐条处理，**保持 JSON 合法**（改完用 `json.loads` 自校验通过再继续）。

1. **title**：事件本身，谁+做了什么（如"SpaceX 完成收购 Cursor"）。中文、≤30 字、结尾**无标点**、零修饰；英文源标题译中文，产品名/模型名（GPT-5.6、Cursor）保留
2. **summary**：事实浓缩段，≤80 字、2-3 个事实点：① 是什么 → ② 关键能力/亮点 → ③ 硬数字（最高优先级必保留）→ ④（可选）附加事实。
   **禁空话词**（建议关注/建议XX/可关注/推荐关注/感兴趣可/值得一试/欢迎/敬请期待），禁复述标题，**禁编造**，必须含至少 1 个原文具体信息（数字/名称/结果）
3. 中英文/数字与中文之间加空格（禁"Cursor并入SpaceX"式粘连；"38.3倍"数字+单位属正常写法）
4. 同日同一事件多条 → 只留一条（优先带金额/来源权威的）；再与前 2-3 天比对，同一事件（措辞不同、语序颠倒）也删——采集脚本已自动过滤大部分，此步为人工兜底。删后重排 id（当天日期-001 起）
5. 字段 category/published_at 一律不动；source/source_url 仅限换源（x.com 不可访问时找可访问同事件源）时更新；条数只减不增
6. **commentary（原创评注，2026-08-25 起新增）**：每条在 summary 之外新增 `commentary` 字段，写一段**原创分析**（60~120 字）：这条新闻对谁有用、对行业/用户意味着什么、隐含趋势或取舍。
   - 是**信息增量**（影响/对比/判断），不是复述 summary（禁"这一举措标志着…"式套话）
   - 禁空话词（与 summary 同一套），禁编造、不展望未来
   - 署名 source/source_url 保持不动；存量历史 news_*.json 无此字段不补（模板已兜底渲染），只对当天新增条目写

### 红线
- 数据必须来自 aihot API 当天最新报道，改写出处不换、事实不编。
- 快讯不是文章：摘要一句话扫完即懂，绝不展开成段落。
- 门禁 `check_news_quality.py` 在 build 时自动跑（只报告不阻断）：硬伤（空摘要/中英文粘连等）会打印警告，按提示修复当天文件即可；评注门禁同套（过短/空话词/复述摘要）。
