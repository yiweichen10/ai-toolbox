# AI 工具助手 & 口碑体系 · 推进清单

> 最后更新：2026-08-08
> 状态：✅ 已完成 ｜ ⏳ 进行中 ｜ ⬜ 待办
> 原则：站在用户角度；每一项都写清「为什么做 / 怎么做 / 验收标准」，避免做一半丢记忆。

---

## ✅ 已完成

- **AI 助手挂件（/tools/ 页原型）**：左下角悬浮按钮 + 抽屉面板，流式回答、快捷问题、工具站内链接、暗色适配、移动端布局实测通过。
- **后端服务**：基于 data/tools/*.json 分片的检索（关键词 + 分类别名 + 意图），GLM-4.7-Flash 流式代理，同 IP 限流 + 全局并发 1 排队，问答 JSONL 日志。
- **口碑推荐 v2**：删除"付费减分"；评分 = 相关性 + 口碑分（编辑评分/访问热度/用户点赞）；模糊查询按综合口碑排序；二元分词过滤虚词噪音。
- **双轨推荐**：用户未明确要免费时，回答默认分「免费/高性价比」+「功能最强/预算充足」两档；检索保底保证候选里免费/付费都有。
- **点赞体系**：/tools/ 列表页 541 行 + 全部工具详情页；防刷（同 IP 同工具一次、同浏览器一次、每 IP 每日上限 25、爆发告警日志）；数据存 `/var/www/aitoollab/data/tool_likes.json`（权限 600，独立于部署目录）。
- **详情页操作按钮版式统一**：主/次按钮同高度同圆角，移动端主按钮独占一行、次按钮三等分。
- **Codex 免费版价格更新**：库内价格改为"ChatGPT 免费版可用（限量试用）+ Go $8 / Plus $20 / Pro $100-$200"。

---

## ⬜ 待办（按优先级）

### P0 · 体验与成本（建议最先做）

**1. 常见问题缓存** ✅ 已完成（2026-08-08）
- 为什么：免费档并发只有 1、高峰期常 429；高频问题（如"免费的AI写作工具"）重复调上游又慢又烧配额。
- 怎么做：后端对问题做归一化后查缓存（按「检索候选 slug + 问题」做 key），命中直接返回；缓存答案带过期时间（如 6-24 小时）与"来自缓存"标记；日志统计命中率。
- 落地：仅缓存"首轮无历史 + 非 mock"的成功回答；key 为归一化问题；默认 TTL 6 小时、上限 200 条（超出淘汰最旧一半）；命中不占上游并发；`/api/health` 暴露命中/未命中统计。
- 验收 ✅：线上实测同一问题两次——第一次 18.5s（调 GLM），第二次 0.16s（缓存命中）；日志记录 `miss`/`hit`，health 统计 hits=1。

**2. 429 排队体验** ✅ 已完成（2026-08-08）
- 为什么：现在撞上限时挂件显示"开小差了"，用户会以为是坏了。
- 怎么做：后端 429 时返回明确的排队语义；前端显示"前面还有人在咨询，正在排队…"并自动重试（指数退避，最多 3 次）。
- 落地：HTTP 429/503 与流式错误里含"429/繁忙/访问量过大"都触发重试；重试间隔 2.5s/5s/10s，最多 3 次；重试期间气泡显示"前面还有人在咨询，正在排队…（第 N 次重试）"。
- 验收 ✅：假后端连续 2 次 429 后，第 3 次自动重试成功输出答案。

**2b. 免费模型 429 自动降级（模型热备）** ✅ 已完成（2026-08-08）
- 为什么：glm-4.7-flash 免费档并发只有 1，高峰期 429 频繁，用户实测直连就撞限流。
- 怎么做：后端把主模型 + 备用免费模型组成降级链：上游 429/5xx 时标记该模型冷却 30 秒 → 短退避（1s/2s/4s）→ 自动切换到下一个免费模型继续回答，并追加"已自动切换轻量模型"提示；全部失败才向用户返回友好错误。
- 落地：备用模型 glm-4-flash / glm-4-flash-250414（并发 5，已 curl 验证可用）；冷却表避免反复打已限流模型；日志新增 model/fallbacks 字段；`/api/health` 暴露备用模型列表；日志分析报告新增"模型使用与降级统计"。
- 验收 ✅：本地假上游回归——主模型 429 → 自动用备用模型答完并出提示；三个模型全 429 → 返回"当前咨询人数较多…"友好错误、不泄露上游 body；日志正确记录 model=glm-4-flash、fallbacks=1。

**2c. 千问/DeepSeek 接入（可切换 + 跨提供商降级）** 🔜 代码就绪（2026-08-08），待填 Key 上线
- 为什么：千问 3.8-Max 已上线（2.4T MoE 旗舰，中文/Agent 能力强）；千问平台同时托管 DeepSeek-V4-Flash（输入 ¥1/M、输出 ¥2/M，比 qwen3.8-max 便宜 10 倍以上，适合免费问答高并发）。DashScope 提供 OpenAI 兼容接口，后端可零协议改动接入。
- 怎么做：新增 `DASHSCOPE_API_KEY` / `QWEN_MODEL`（默认 qwen3.8-max）/ `QWEN_MODEL_FALLBACKS`（默认 qwen-plus,qwen-turbo）/ `QWEN_API_BASE` / `QWEN_ENABLE_THINKING`（默认关）；新增 `DEEPSEEK_API_KEY` / `DEEPSEEK_USE_DASHSCOPE`（置 1 时复用百炼 Key 走千问平台）/ `DEEPSEEK_MODEL`（默认 deepseek-v4-flash）；提供商顺序为 DeepSeek → 千问 → 智谱，各自内部再按主/备用模型降级；`/api/health` 暴露 `provider` 字段。
- 落地：`server.py` 多提供商流式调用（本地假上游已验证：DeepSeek 主用、千问 429 → 自动降级到智谱答完）；`deploy_assistant.sh --qwen-key` 写入 DashScope Key；前端无改动。
- 待办：在阿里云百炼领取 API Key → 服务器配置 DASHSCOPE_API_KEY + DEEPSEEK_USE_DASHSCOPE=1 → 重启 → 观察 `/api/health` 显示 provider=deepseek。

**3. 日志轮转 & 点赞数据备份确认** ✅ 已完成（2026-08-08）
- 为什么：问答日志会无限增长；点赞/反馈是口碑资产，不能因服务器故障丢失。
- 怎么做：配置 logrotate（日志按天切分、保留 30 天）；确认 `/var/www/aitoollab/data/` 在服务器备份（hbrclient）范围内，不在则加定时打包。
- 落地：`/etc/logrotate.d/aitoollab`（每日切分、保留 30、压缩、copytruncate）；`/opt/aitoollab-assistant/backup_data.sh` + `/etc/cron.d/aitoollab-data`（每天 03:00 打包 data 目录，保留 14 份）。服务器另有 hbrclient（阿里云备份客户端）运行，本地定时打包作为双保险。
- 验收 ✅：logrotate dry-run 通过（识别 access/ai_assistant/error 日志）；手动跑备份生成 `data_20260808_102212.tar.gz`。

> 附带修复（2026-08-08）：发现部署脚本漏同步 `data/tools.json`（本地 532 款、服务器 529 款），已手动同步并把 `data/tools.json`、`data/articles.json` 加入 deploy.sh 常同步列表，防止再次漂移。

### P1 · 核心体验

**4. AI 回答反馈按钮（有用/没用）** ✅ 已完成（2026-08-08）
- 为什么：直接收集"这个回答好不好"，是调优检索/提示词的第一手数据，也是第二个口碑信号。
- 怎么做：
  - 位置：每条 AI 回答气泡底部一行小按钮 👍 有用 / 👎 没用（类似 ChatGPT）。
  - 后端：生成 `answer_id` 随流式回答下发；`POST /api/feedback {answer_id, value, token}`；数据存 `/var/www/aitoollab/data/ai_feedback.json`（权限 600，独立于部署目录），字段含问题、回答摘要、反馈值、时间、IP 哈希。
  - 防刷：同 IP 限频；同一 answer_id + 同一浏览器只能反馈一次，允许改一次（有用↔没用）。
- 落地：`answer_id` 随流式回答下发（缓存命中也会生成新的）；每条回答底部渲染反馈行；同身份可改票；问题原文由后端"answer_id→问题"近期映射自动补齐；数据 600 权限独立存储。
- 验收 ✅：线上真实问答→拿到 answer_id→反馈写入，日志带问题原文；前端假后端实测点 👍 即时高亮；首页/详情页挂件均正常。

**5. 挂件全站铺开（首页 / 工具详情页 / 分类页）** ✅ 首页+详情页完成（分类页待定，2026-08-08）
- 为什么：现在只有 /tools/ 有入口，用户从别处进来找不到 AI 助手。
- 怎么做：挂件样式抽成独立文件 `css/ai-widget.css`（内容哈希自动缓存版本），首页/详情页模板注入 link + script；点赞按钮样式仍留在原页面。
- 落地：首页、全部工具详情页已上线；分类页（category/**）暂未注入，等观察首页+详情页负载后再决定。
- 验收 ✅：线上首页与详情页均有挂件、样式文件可访问、无重复初始化、零控制台报错。

**6. 会话记忆（刷新不丢）** ✅ 已完成（2026-08-08）
- 为什么：现在对话只存内存，刷新就断，用户连续咨询体验差。
- 怎么做：前端把最近 10 条消息（约 5 轮）存浏览器本地存储，刷新后恢复上下文（后端本就支持传 history）；面板头部加"清空对话"按钮。
- 落地：`ai_chat_history_v1`；恢复时不重复显示欢迎语；清空按钮同时清本地存储与消息区。
- 验收 ✅：问答后刷新页面，对话完整恢复；清空后消息区归零。

### P2 · 质量加固

**7. 幻觉后校验** ✅ 已完成（2026-08-08）
- 为什么：提示词已约束"只能推候选清单"，但 LLM 偶尔仍可能输出库外工具或编造价格，需要最后一道闸。
- 落地：后端在流式结束后扫描回答里的 `/tools/{slug}/` 链接，指向"不存在的工具"的链接自动降级为纯文本，并向用户追加一条"已自动修正"提示；缓存存修正后的干净文本（后续命中不再出现坏链）。
- 验收 ✅：单元测试确认库内链接保留、库外链接被移除并记录修正列表；前端实测修正提示正常渲染。

**8. 详情页语境化提问** ✅ 已完成（2026-08-08）
- 为什么：用户在详情页时意图更明确，预置相关问题转化率更高。
- 落地：详情页 `<body>` 注入 `data-ai-tool` / `data-ai-tool-slug`，挂件据此把快捷问题替换为"XX 好用吗？适合什么人用？/ XX 有免费版吗？/ XX 有哪些替代工具？"。
- 验收 ✅：进入 ChatGPT 详情页实测快捷问题随工具名变化；首页不显示语境问题。

**9. 别名/标签持续进化（数据驱动）** ⏳ 工具已就绪 + 首份报告（2026-08-08）
- 为什么：检索质量的上限由别名表和标签决定，真实问答日志是最好的来源。
- 怎么做：定期（每周）从日志里筛"候选不理想/回答被点没用"的查询，提炼新别名、补标签；沉淀到数据文件。
- 落地：`scripts/analyze_chat_logs.py` 一键生成分析报告（提问量/缓存命中率/高频问题/检索薄弱问题/负面反馈问题/别名建议），首份报告已产出（`reports/ai-assistant-analysis-2026-08-08.md`）。
- 下一步：每周跑一次报告 → 人工复核"别名建议" → 补入 `CATEGORY_ALIASES` 或工具标签 → 用同一批历史问题回归验证。

### P3 · 稳健性

**10. 健康告警** ✅ 已完成（2026-08-08）
- 为什么：服务挂了目前只有 systemd 自重启，没人主动知道。
- 怎么做：服务器定时任务每 5 分钟检查公开 `https://www.aitoollab.cn/api/health`（覆盖 nginx+SSL+反代+助手），连续失败 3 次发钉钉告警、持续故障每 30 分钟重复提醒、恢复后发"已恢复"；同时读本地证书算剩余天数，<14 天每天提醒一次。
- 落地：`ai_assistant/health_alert.py`（零依赖 Python3.6，支持钉钉普通/加签两种模式，本地 JSONL 日志 + 状态文件防重复轰炸）；`/etc/cron.d/aitoollab-health` 每 5 分钟执行；Webhook 经 stdin 写入 `/etc/aitoollab/health-alert.env`（600 权限），本地命令 `echo 'webhook' | bash scripts/set_dingtalk_webhook.sh`。
- 验收 ✅：本地回归全部通过（连续失败 3 次→告警、恢复→通知、证书剩 9 天→每天一次、加签 URL 正确）；钉钉加签 Webhook + 密钥已配置（`/etc/aitoollab/health-alert.env` 权限 600，密钥不进 git）；`health_alert.py --test` 测试告警返回 `sent`，钉钉群已收到；cron 每 5 分钟健康检查日志持续写入。

---

## 📋 事故记录

- **2026-08-08 SSL 证书过期事故**：网站短暂无法访问。WorkBuddy 已重新签发证书（有效期至 2026-11-06）并开启 certbot 自动续签（`certbot-renew.timer` 已确认存在，每天两次）。经验：certbot 续签定时任务存在≠一定成功，需在 10 月初（到期前 30 天）验证首次自动续签是否生效；P3 第 10 项将加入证书到期监控兜底。

---

## 说明

- 每一项完成后回来勾状态（✅）并补一句"做了什么/验证结果"，保持清单即记忆。
- 优先级可随真实数据调整：日志里"没用"率高的方向优先做。
