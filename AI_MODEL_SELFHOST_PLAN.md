# 自建大模型规划：从「用别人的」到「自己的」

> 目标：先明确自训/自部署的路线图、模型规模选型，并一并解决 Hermes 等 Agent 工具因 DeepSeek 涨价带来的费用压力。
> 数据基准日：2026-08-21（DeepSeek 8/17 调价已生效；Qwen3.8-27B 已于 2026-08-14 开源）。

---

## 一、结论先行（少废话版）

1. **现在不该「从零自训」**，成本与周期都不可控。正确路径是：**自部署开源权重 → 用我们的数据做垂直微调（LoRA）→ 未来再考虑继续预训练/自训**。
2. **起步规格：单卡 RTX 4090（24GB）跑 Qwen3.8-27B（Q4_K_M 量化，~17.5GB 权重）**。这是 2026-08-14 刚开源的 Apache-2.0 权重，27.78B 稠密、原生多模态（图/视频）、262K 上下文、原生工具调用；能力介于 14B 与 32B 之间但单卡可跑，比 14B 强一截、比 32B 省显存，是性价比拐点。7B 太弱（复杂 Agent 链掉调用精度），32B 是复杂 Agent 首选但显存吃紧。
3. **Hermes 费用问题本质是用 OpenAI 兼容 API 切换后端**：自部署一个暴露 `/v1/chat/completions` 的模型（vLLM/Ollama），把 Hermes 的 `base_url` 指过去，代码零改，费用从「按 token 计费」变成「固定算力费 ≈ 0 边际成本」。**Qwen3.8-27B 已被 Hermes Agent 在生产环境（via OpenRouter）验证格式兼容，切换零风险。**
4. **打平点**：Hermes 类 Agent 月输出 token 超过 ~20–40M（按 DeepSeek 空闲价算）时，自部署固定算力费即低于 API 费；超过越多越省。

---

## 二、现状痛点：DeepSeek 8/17 涨价冲击

DeepSeek V4 全系列 2026-08-17 0 点生效峰谷定价（高峰 = 北京 9:00–12:00、14:00–18:00，其余空闲半价）：

| 模型 | 项目 | 涨价前(元/百万) | 高峰(元/百万) | 空闲(元/百万) | 涨幅 |
|------|------|---------|---------|---------|------|
| V4-Flash（我们 AI 助手现用） | 输出 | 2 | **9** | 4.5 | +350% |
| V4-Flash | 输入(未命中) | 1 | 3 | 1.5 | +200% |
| V4-Flash | 输入(命中) | 0.02 | 0.10 | 0.05 | +400% |
| V4-Pro | 输出 | 6 | **27** | 13.5 | +350% |

- 你站上 AI 助手走的是 **DashScope 平台托管的 deepseek-v4-flash**，价格跟随阿里百炼体系，不一定等同 DeepSeek 官方，但同趋势上行。
- **Hermes 等 Agent 直接用 DeepSeek 官方 API**，首当其冲。峰谷叠加后，高峰时段成本接近原来的 3–12 倍（缓存命中输入涨幅最高达 1100%）。
- 行业背景：2026 Q2 中国大模型平均输出价已涨到 21.9 元/百万（较 2025 Q1 的 12.2 涨 80%），涨价是行业性、持续性趋势，不会回头。

---

## 三、模型规模选型：上多大合适？

Agent / 工具调用场景的社区实测结论（r/LocalLLaMA、OpenClaw 实测、BFCL 榜单、Qwen3.8-27B 发布实测）：

| 规模 | 代表模型 | 量化后显存 | 工具调用能力 | 适用场景 | 硬件 |
|------|---------|-----------|------------|---------|------|
| 3B | Llama-3.2-3B | ~1.8G(Q4) | ⭐⭐⭐⭐ | 边缘/低配，简单分类 | 4G 显存 |
| **7B** | **Qwen2.5-Coder-7B / Qwen2.5-7B-Instruct** | ~4G(Q4) | ⭐⭐⭐⭐⭐ | AI 助手/简单 Agent，最省 | 3090/4090 |
| **14B** | **Qwen2.5-Coder-14B / Qwen3-14B** | ~9G(Q5) | ⭐⭐⭐⭐⭐ | 中文+工具调用甜点，轻量备选 | 单卡 4090/A100 |
| **27B（推荐起步）** | **Qwen3.8-27B（Apache-2.0，原生多模态+工具调用）** | ~17.5G(Q4_K_M) / ~20.3G(Q5_K_M) | ⭐⭐⭐⭐⭐ | 单卡 4090 跑得起、能力接近 32B、Hermes 已在生产验证 | 单卡 4090(24GB)/5090 |
| 32B（复杂 Agent 首选） | Qwen2.5-Coder-32B / Qwen3-32B | ~20G(Q4) | ⭐⭐⭐⭐⭐ | 多步 Agent 编排、规划 | 单卡 4090(紧)/A100 |
| 70B+ | Llama-3.3-70B / DeepSeek-V3 量化 | 24G+(激进量化) | 强 | 不必要，成本陡增、收益边际 | 多卡/Server |

**选型修正（为什么把起步从 14B 上调到 27B）**
- Qwen3.8-27B 是 2026-08-14 新鲜开源的 **Apache-2.0** 权重（商用无虞），27.78B 稠密 + 混合注意力（64 层中仅 16 层全注意力），所以 4-bit 能压到 ~17.5GB，单卡 4090 跑得动。
- 它**原生工具调用、JSON、OpenAI 兼容 Chat Completions**，且公开资料显示 **Kilo Code、Zed、pi、Hermes Agent 已通过 OpenRouter 在生产使用**——意味着我们把 Hermes 的 `base_url` 指向自托管 27B，tools 格式完全对齐，零适配风险。
- 附带多模态：自带视觉编码器（图/视频理解、computer-use/browser-use），未来 AI 助手要做「截图问答/文档识别」不用另接视觉模型。
- 262K 原生上下文（YaRN 扩到 1M），长 Agent 链/长文档场景余量足。
- 代价：比 14B 多吃 ~8GB 显存（4090 仍装得下），租卡费与 14B 同档（按卡计费不看模型大小）。

**针对我们的两个用途的决策：**
- **aitoollab AI 助手（工具推荐/分类）**：7B–14B 足够。推荐 Qwen2.5-7B-Instruct 起，后续用我们数据微调到 7B 专用小模型，可降到边缘设备。
- **Hermes 等 Agent 编排（统一后端）**：**直接上 Qwen3.8-27B（Q4_K_M）**。它是单卡 4090 能跑的最大实用档，原生工具调用 + 已被 Hermes Agent 生产验证，切换零风险。复杂场景仍可上 32B。

**许可提醒**：Qwen 系列 Apache-2.0（可商用友好，27B 即采用）；DeepSeek 权重为自有许可需确认商用条款；GLM 系列需单独确认商用授权。**起步优先 Qwen，规避许可风险。**

### 选型修正补充：Qwen3.8 家族到底有几个开源档？（2026-08-21 核实）

**结论：Qwen3.8 开源权重只有两档，没有 32B。**

| 档位 | 参数量 | 架构 | 许可 | 多模态 | 显存/硬件 | 适合谁 |
|------|--------|------|------|--------|-----------|--------|
| **Qwen3.8-27B** | 27.78B 稠密 | 64层（仅16层全注意力）+ 27层视觉编码器 | **Apache-2.0** | 图/视频原生 | Q4_K_M ~17.8GB → **单卡 4090(24GB) 可跑** | ✅ 我们的起步档 |
| **Qwen3.8-2.4T-A95B**（即 Max 开源底座） | 2.4T 总 / **95B 激活** / 512专家 | MoE | **Qwen3.8-Max 定制许可**（非 Apache） | 纯文本（无视觉栈） | BF16 ~4.4TB；4-bit >1TB；**需 450GB+ 显存的多卡集群（H100/H200/MI300X）** | ❌ 我们够不着 |

关键事实（多源核实 2026-08-21）：
- **无 32B 开源权重**：Qwen3.8 代只有 27B 与 2.4T MoE 两个开放权重。想要 32B 量级只能退回上一代 **Qwen3-32B / Qwen2.5-Coder-32B**（虽开源但更老、Agent/编码弱于 3.8-27B，不推荐）。
- **2.4T 的坑**：① 定制许可——年营收 >5000万美元 的 MaaS/AI 助手业务需向千问单独申请许可；② 纯文本、无视觉；③ **thinking 不可关**（每答必付推理 token）；④ 显存门槛极高（450GB+ RAM，多卡 H100/H200/MI300X + SGLang/vLLM 张量+专家并行）。
- **API-only 档（无权重）**：Qwen3.8-Max 托管版、Qwen-Plus/Max（3.7/3.8）等只有 API、不开放权重，不在自部署范围。
- **为什么 27B 是拐点而非越大越好**：官方基准称 27B 在 SWE-bench Pro / Terminal-Bench / JobBench 等 Agent 编码榜上**超过更大的 Qwen3.7-Plus**；单卡 4090 可跑意味着固定算力费最低。在单卡预算内，27B 已是能力上限，上 2.4T 是另一个数量级的成本（见成本精算）。

---

## 四、成本对比：API vs 自部署（精算）

GPU 租赁实时价（用户给定锚点，2026-08-21；汇率按 ¥7.2/$ 估算，部署前复核当日报价）：

| GPU | 显存 | RunPod Community | RunPod Secure | Vast.ai | 国内 AutoDL |
|-----|------|-----------------|--------------|---------|-----------|
| RTX 4090 | 24GB | $0.34/hr | $0.69/hr | $0.20–0.55 | ¥0.28/hr |
| RTX 3090 | 24GB | $0.22/hr | $0.43/hr | $0.27 | ¥0.19/hr |
| A100 80GB | 80GB | $1.19/hr | $1.89/hr | $0.67–1.89 | ¥0.48/hr |
| H100 | 80GB | $2.49/hr | $3.49/hr | $2–3.5 | — |

### 4.1 单卡 4090 24/7 常驻月费（720 小时/月）

| 平台 | 单价 | 月费(USD) | 月费(RMB @7.2) |
|------|------|-----------|----------------|
| **国内 AutoDL 4090** | ¥0.28/hr | — | **¥201.6** |
| RunPod Community 4090 | $0.34/hr | $244.8 | ¥1,762.6 |
| RunPod Secure 4090 | $0.69/hr | $496.8 | ¥3,577.0 |
| Vast.ai 4090（区间） | $0.20–0.55/hr | $144–396 | ¥1,036.8–2,851.2 |

> **结论：常驻档 AutoDL ¥201.6/月 是绝对最优**（国内无跨境延迟、价格最低 6–9 倍）。RunPod 仅在你无法用国内卡、且月输出 >300M token 时才划算。

### 4.2 DeepSeek V4-Flash 月 API 费（仅输出 token，RMB）

峰谷价：高峰（京 9–12、14–18，共 7h/天）9元/M；空闲（其余 17h/天）4.5元/M。
混合价 = 9×7/24 + 4.5×17/24 = **5.82 元/M**（按小时加权）。

| 月输出 token | 全空闲(4.5) | 全高峰(9) | 混合(5.82) |
|--------------|-------------|-----------|------------|
| 20M | ¥90.0 | ¥180.0 | ¥116.4 |
| 40M | ¥180.0 | ¥360.0 | ¥232.8 |
| 50M | ¥225.0 | ¥450.0 | ¥291.0 |
| 100M | ¥450.0 | ¥900.0 | ¥582.0 |

> 注：输入 token 约 1元/M（命中缓存 0.05元/M），Agent 场景输出占大头，上表已覆盖主要成本；若要精确再加输入费（通常 < 输出的 20%）。

### 4.3 自部署 vs DeepSeek API（打平对比，以 AutoDL 为例）

| 月输出 token | DeepSeek 混合价 | AutoDL 固定(¥201.6) | 自部署每月净省 |
|--------------|-----------------|---------------------|----------------|
| 20M | ¥116.4 | ¥201.6 | **-¥85.2**（DeepSeek 更省） |
| 40M | ¥232.8 | ¥201.6 | +¥31.2 |
| 50M | ¥291.0 | ¥201.6 | +¥89.4 |
| 100M | ¥582.0 | ¥201.6 | +¥380.4 |

**精确打平点（AutoDL 固定费 ÷ DeepSeek 单价）：**
- 混合价 5.82元/M → **34.6M 月输出 token** 打平
- 全高峰 9元/M → 22.4M 打平
- 全空闲 4.5元/M → 44.8M 打平

✅ **你原话"月输出 >~20–40M token 打平"在 AutoDL 档精确成立**：高峰价下 22.4M 即打平，混合价下 34.6M 打平。

> 打平点之上，自部署 token 无限量、边际成本≈0；DeepSeek 线性上涨。例：月输出 100M，AutoDL 方案比 DeepSeek 省 ¥380/月（≈ 2 倍差价），且 Hermes + 站点 AI 助手 + 其他 Agent 可共用同一端点，DeepSeek 费按各工具 token 累加，自部署固定费被摊薄 → **实际打平点进一步下探到 ~15–25M**。

### 4.4 低利用率场景（更省）

若 Hermes 并非 24/7 满跑，按实际开机时长计费：

| 使用时长 | AutoDL 月费 | 对应打平点(混合价) |
|----------|-------------|--------------------|
| 24/7（720h） | ¥201.6 | 34.6M |
| 8h/天（240h） | ¥67.2 | ~11.5M |
| 4h/天（120h） | ¥33.6 | ~5.8M |

- 用 **RunPod Serverless / Modal 按秒计费**：idle 成本归零，调用稀疏时比 24/7 再降一档。
- 本地买卡（4090 ¥1.3–1.5万）：高利用率下 2 年 TCO 最低，但需自管运维/断电，适合 Phase 2 稳定后。

---

### 4.5 更高版本（2.4T MoE）的代价（为什么现在不碰）

| 项目 | Qwen3.8-27B（我们选） | Qwen3.8-2.4T-A95B |
|------|----------------------|-------------------|
| 月算力费（自部署） | ¥201.6（单 4090） | 多卡 H100/H200 集群，约 **¥3万–10万+/月** |
| 显存 | 24GB | 450GB+（BF16 4.4TB） |
| 许可 | Apache-2.0 | 定制，年营收>5000万美元 MaaS 需单独许可 |
| 多模态 | 原生图/视频 | 纯文本 |
| thinking | 可关 | 不可关 |
| 打平点 | 22–45M 月输出 | 不切实际（需数百 M 级） |

> 关键认知：自部署把「变动成本（按 token）」变成「固定成本（算力）」。Hermes 一旦高频使用，边际成本趋零；DeepSeek 则线性上涨无上限。在单卡预算内 27B 即能力天花板，2.4T 是另一个数量级的成本，等真有集群预算再考虑。

---

## 五、Hermes 费用归零方案（本周可做）

Hermes 等 Agent 框架后端是 OpenAI 兼容 API。自部署模型暴露同构 endpoint 即可无缝切换：

```
Hermes 配置改两行：
  base_url = http://你的服务器:8000/v1   # vLLM / Ollama OpenAI 兼容
  api_key  = sk-任意（自托管不校验）
  model    = qwen3.8:27b
```

1. **部署推理服务**：`vLLM`（生产，吞吐比 Ollama 高 2.3×，适合并发）或 `Ollama`（入门快）。
   - `python -m vllm.entrypoints.openai.api_server --model Qwen/Qwen3.8-27B --tensor-parallel-size 1 --gpu-memory-utilization 0.9 --max-model-len 262144`
   - 注：262K 上下文很吃 KV cache，4090 上短上下文/低并发跑 Q4_K_M 留足余量；要长上下文用 5090/48GB 或降并发。
2. **对齐 tools 格式**：Hermes 用 OpenAI `tools`/`function` schema，Qwen3.8-27B 原生支持，无需 prompt hack（且已在 Hermes Agent 生产验证）。
3. **降级兜底**：保留 DeepSeek 官方/DashScope 作为 fallback（API 里已有 provider 抽象，复用即可）。
4. **验证 golden set**：用 50–200 条真实工具调用场景做回归（参考 LocalLLaMA 实践），确认工具调用准确率不退化。

---

## 六、分阶段路线图

### Phase 0 — 零成本验证（本周，1–2 天）
- 云上起 1× 4090（AutoDL ¥0.28/hr 或 RunPod），部署 Qwen3.8-27B（Q4_K_M，~17.5GB 权重）。
- 把 Hermes base_url 指过去，跑通工具调用；同步给 aitoollab AI 助手加一个「自托管 provider」选项（server.py 已有 provider 抽象，加一段即可）。
- **产出**：费用归零验证 + 工具调用准确率基线。

### Phase 1 — 垂直微调（1–2 月）
- 用现有资产造 SFT 语料：
  - **662 工具结构化数据**（名称/分类/功能/FAQ）→ 工具推荐样本
  - **文章 + 真实对话日志**（含你刚问的 GLM 5.3 那 4 条）→ 多轮对话样本
- LoRA 微调 7B/14B（单卡 4090 即可，显存占用小），打造「AI 工具推荐」专用小模型；27B 也可做 QLoRA 微调（Unsloth 宣称 2× 快、70% 省显存）。
- **产出**：专用小模型，AI 助手可降到 7B 跑在更便宜的卡/边缘。

### Phase 2 — 生产化（3–6 月）
- vLLM 多卡/量化部署 + KV cache 优化；自建评测集（golden set）持续回归。
- 监控：工具调用准确率、降级率、P99 延迟；Hermes 与 AI 助手共用同一自托管端点，统一降级链。
- **产出**：稳定生产服务，DeepSeek 仅作 fallback。

### Phase 3 — 自训（长期，谨慎）
- 仅当数据/算力/标注积累到阈值（百万级高质量垂直样本 + 多卡 A100/H100 预算）再考虑继续预训练或从头训。
- 短期不推荐：全参训练成本与风险极高，且开源权重已很强。**先把 Phase 0–2 跑通，现金流为正。**

---

## 七、数据飞轮（我们已具备的稀缺资产）

| 资产 | 规模 | 可用于 |
|------|------|-------|
| 工具结构化数据 | 662 工具 | 工具推荐 SFT、检索召回 |
| 文章/词典 | 165+ 篇 / 124 词条 | 领域知识增强、RAG |
| 真实对话日志 | 持续增长（如 ai_assistant.log） | 多轮对话微调、评测 golden set |
| 中文 SEO 站流量 | 真实用户 | 持续产生新对话样本 |

这些正是「通用模型没有、我们有」的垂直语料，是未来自训模型的核心壁垒。现在每一条对话日志都在为将来攒训练数据。

---

## 八、风险与坑

1. **小模型复杂链掉精度**：7B 在多步 Agent 链上工具调用准确率明显低于 32B。务必用 golden set 实测，不要只看 benchmark。
2. **量化别过低**：27B 在 4090(24GB) 上 **Q4_K_M（~17.5GB）最稳**，留 KV 余量；要更高精度（Q5_K_M ~20.3GB）需 5090/48GB 卡。Agent 场景避免低于 Q4_K_M，否则 schema 漂移、并行/嵌套参数出错。
3. **Hermes 切换需对齐 tools 格式**：先小流量验证，保留 DeepSeek fallback（27B 与 Hermes 已生产验证，风险低）。
4. **许可**：商用优先 Qwen（Apache-2.0，27B 即采用）；GLM/DeepSeek 商用前确认条款。
5. **隐私**：自托管数据不出域是卖点，但需关 telemetry（OLLAMA_OFFLINE=1、HF_HUB_DISABLE_TELEMETRY=1）。
6. **算力稳定性**：云 4090 Community 可能被抢占，生产用 Secure 或 AutoDL 稳定实例；本地买卡则 2 年 TCO 最划算（4090 ¥1.3–1.5万，高利用率回本快）。
7. **长上下文吃显存**：262K 上下文在 4090 上 KV cache 会占数 GB，长会话需降并发或上更大显存卡。

---

## 九、本周行动清单

- [ ] 租用 1× 4090（AutoDL 或 RunPod），部署 Qwen3.8-27B-Q4_K_M
- [ ] 起 vLLM/Ollama OpenAI 兼容端点，本地 curl 验证 tools 调用
- [ ] 改 Hermes base_url 指向自托管 27B，跑一条真实 Agent 任务
- [ ] aitoollab AI 助手 server.py 加「自托管 provider」+ 保留 DeepSeek 降级
- [ ] 导出 662 工具 + 对话日志，设计 SFT 样本模板（Phase 1 准备）

---

*本规划基于 2026-08-21 联网核实的 DeepSeek 调价、Qwen3.8-27B 规格与实测、开源模型 Agent 实测、GPU 租赁价格。GPU/API 价格随市场波动，执行前复核当日报价。*
