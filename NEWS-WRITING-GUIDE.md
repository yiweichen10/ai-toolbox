# AI 快讯写作规范（价值提炼版）

> 目的：让「AI 快讯」从"新闻搬运"变成"价值简报"——每条都必须回答「这对我意味着什么」。
> 本规范用于 Codex 快讯自动化（每天 07:52）的生成 prompt，也可粘贴到任何快讯生成指令。

## 一、定位（最重要的一条）

**快讯不是新闻列表，是「今天 AI 圈值得你关注的 N 件事」。**

读者（来找 AI 工具的人 + AI 从业者）不缺新闻源，缺的是**信息增量**：
- 这件事跟我有什么关系？
- 我该关注、该试、该避开什么？

所以**摘要绝不允许复述新闻事实**，必须给出「价值判断」。

## 二、标题规范

| 项 | 要求 |
|----|------|
| 语言 | 中文（英文源标题必须译中文，但保留关键英文产品名：GPT-5.6 / Cursor / DeepSeek） |
| 长度 | ≤ 40 字 |
| 结构 | 主体 + 动作 + 结果/影响 |
| 标点 | 结尾不加句号/感叹号等 |

- ❌ 反例：`OpenAI and Anthropic in price war as Chinese AI rivals gain ground`（英文）
- ❌ 反例：`从0到1带你速通DeepSeek Harness。`（结尾句号 + 空话）
- ✅ 正例：`OpenAI 与 Anthropic 掀价格战：GPT-5.6 降价 80%`
- ✅ 正例：`通义千问开源 Qwen3.8：27B 参数原生多模态，超越前代`

## 三、摘要规范（核心）

| 项 | 要求 |
|----|------|
| 长度 | 一句话，≤ 60 字 |
| 内容 | 回答「这对我意味着什么 / 我该做什么」，**不是复述新闻** |
| 硬信息 | 必须含数字 / 结果 / 价格 / 影响 之一 |
| 可选格式 | 价值钩子开头，如「利好：」「对开发者：」「注意：」 |

- ❌ 反例（复述式）：`OpenAI 宣布 GPT-5.6 Luna 降价 80%，输入价从 1 美元降至 0.2 美元，Anthropic 推出 Claude Opus 5 半价。`
- ✅ 正例（价值式）：`利好开发者：GPT-5.6 输入价砍到 0.2 美元/百万 token（降 80%），用 OpenAI API 的成本大幅下降，可考虑回流。`

- ❌ 反例：`Cursor 已被 SpaceX 正式收购，完成自 4 月启动的收购流程。`（复述流程，无价值）
- ✅ 正例：`注意：Cursor 并入 SpaceX 后将获 GPU 集群、模型更强更便宜，现有订阅用户暂不受影响，可继续用。`

## 四、分类（五选一）

`models 模型发布` / `products 产品发布` / `industry 行业动态` / `opinion 观点` / `paper 论文研究`

## 五、硬性红线（门禁会拦截）

1. `summary` **必须非空**——空摘要直接导致卡片"标题+来源"无内容，是硬伤。
2. `title` **必须非空、中文、结尾无标点**。
3. 每条**尽量关联站内工具页**（新闻里出现的模型/产品，若站内有工具页，摘要里给出站内链接路径）。
4. 英文源标题必须译中文，不能整句照搬英文。

## 六、一条完整正例

```json
{
  "title": "OpenAI 与 Anthropic 掀价格战：GPT-5.6 降价 80%",
  "summary": "利好开发者：GPT-5.6 输入价砍到 0.2 美元/百万 token（降 80%），Anthropic 推 Opus 5 半价，用 API 的成本大幅下降，可考虑回流或换便宜模型。",
  "category": "industry",
  "source": "Ars Technica",
  "source_url": "https://..."
}
```

## 七、本地门禁（自动兜底）

`scripts/check_news_quality.py` 在 build 快讯前自动跑，硬伤（空摘要/空标题/英文标题）打印醒目警告。
只报告不阻断——根治靠本规范在生成端执行，门禁是最后一道兜底。
