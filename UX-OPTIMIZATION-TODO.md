# 用户体验优化清单（UX Optimization Todo）

> 整理日期：2026-08-09
> 来源：全站走查（首页 / 工具详情 / 文章 / 404，桌面 + 移动端）+ 代码检查
> 更新：2026-08-09 已完成 P0 全部 5 项与 P1 全部 6 项（详见各条目状态）；P2 长期项待产品决策。
> 排序原则：
> - P0 = 用户能直接感知的问题（bug、性能、内容重复），建议优先修复；
> - P1 = 明显提升便利性、成本不高的功能；
> - P2 = 需要产品/后端决策的长期项。

---

## P0 - 建议尽快修复（有实际体验问题）

### P0-1 全站搜索缺失，404 搜索和 Google 站内搜索失效 —— ✅ 已完成（2026-08-09）

**用户现象**
- 404 页搜索框写着"搜索AI工具或文章..."，回车后跳转 `/?q=关键词`，但首页没有任何代码读取 `q` 参数，搜索不会执行，用户会以为功能坏了；
- Google 站内搜索入口（`SearchAction` 结构化数据同样指向 `/?q=`）也会落空；
- 工具详情页、文章页、快讯页都没有搜索框，用户进入深页面后无法搜索；
- 首页搜索只过滤工具卡片（名称/简介/标签），搜不到文章、快讯、词典词条。

**涉及位置**
- `index.html`：SearchAction JSON-LD、`search-bar-below-nav`；
- `404.html`：`onkeydown` 跳转 `/?q=`；
- `js/main.js`：`initSearch()`（约 388 行起），未处理 `location.search`。

**建议方案**
1. 首页加载时读取 `?q=` 并自动填入搜索框、执行搜索；
2. 全站页头统一增加搜索框（跳转 `/?q=` 或全局结果页）；
3. 搜索范围扩展到文章 / 快讯 / 词典（先做标题匹配即可）；
4. 404 搜索改为直接跳全局搜索页。

**验收标准**：`/?q=DeepSeek` 打开首页即出结果；404 搜索可用；任意深页面可搜索。

**预估**：S（约 1 天）

**完成情况**：`main.js` 读取 `?q=` 自动搜索；首页之外全站注入搜索条（GET /?q=）；搜索同时匹配页面内文章链接，无结果时给出文章/快讯/词典入口。构建脚本 `scripts/build.py` 的后处理统一注入（幂等）。

---

### P0-2 页面体积过大，移动端加载慢 —— ✅ 代码侧完成（2026-08-09）

**用户现象**
- "全部工具"页单页 HTML 约 736KB（内嵌 535 条 ItemList JSON-LD）；
- 首页还需下载约 906KB 的 `js/tools-data.js`；
- 手机流量 / 弱网环境下首屏明显变慢，是用户第一时间的体验损失。

**涉及位置**
- `tools/index.html`（736KB）；
- `js/tools-data.js`（906KB）；
- `index.html`（依赖该 JS 渲染首页）。

**建议方案**
1. ItemList 结构化数据移到外部 JSON 或仅保留前 N 条；
2. `tools-data.js` 压缩 / 拆分（按分类懒加载、去重、精简字段）；
3. 确认部署层开启 gzip/brotli 与长缓存；
4. 图片保持 lazy-load（已部分实现）。

**目标**：全部工具页首包 ≤ 250KB；首页首屏 JS 体积减半。

**预估**：M（涉及生成脚本与部署，约 2-3 天）

**完成情况**：`tools-data.js` 紧凑序列化 + 移除无人消费的 `__REMAINING_TOOLS__`，906KB → 365KB（-60%）。`tools/index.html` 保持全量静态内容（SEO 需要），剩余体积依赖服务器 gzip/brotli，部署时确认 nginx 已开启。

---

### P0-3 工具详情页内容重复（FAQ 两遍、相关链接重复） —— ✅ 已完成（2026-08-09）

**用户现象**
- 同一批问题在"常见问题（FAQ）"和"❓ 常见问题"两个区块重复出现，页面观感粗糙；
- "相关对比 / 替代方案"常有同名不同 URL 的重复链接（如 ChatGPT 页出现两条 "ChatGPT vs Claude"）。

**涉及位置**
- 工具页生成模板（样例：`tools/chatgpt/index.html`，FAQ 标题位于约 390 / 438 行）；
- 该问题在抽查的多个工具页中普遍存在（模板级问题）。

**建议方案**
1. 修改生成模板：FAQ 合并为一份；
2. 对比 / 替代链接按「标题 + URL」去重。

**验收标准**：抽查 5 个工具页，无重复 FAQ、无同名重复链接。

**预估**：S（改模板 + 重跑构建）

**完成情况**：正文 markdown 中的 FAQ 小节统一剥离，由模板 `faq-section` 唯一渲染（含 FAQ Schema，问题按规范化去重，正文-only 工具自动兜底）；相关对比/替代链接按标题去重。抽查 7 个工具页全部通过。

---

### P0-4 长文章没有目录，也没有上一篇 / 下一篇 —— ✅ 已完成（2026-08-09）

**用户现象**
- 文章动辄十几小节（如"AI编程助手推荐"），只能一直往下滚，无法快速定位；
- 读完后没有上一篇 / 下一篇，只能退回列表。

**涉及位置**
- 文章生成模板（样例：`articles/ai-coding-assistant-recommend-2026/index.html`）。

**建议方案**
1. 正文前加"本文目录"（自动取 h2/h3 生成锚点）；
2. 文末加上一篇 / 下一篇；
3. 移动端目录做成可折叠面板。

**验收标准**：任选一篇长文，目录可跳转；文末有上下篇链接。

**预估**：S-M（涉及文章模板批量重生成）

**完成情况**：正文 ≥3 节自动生成"本文目录"（h2/h3 锚点，排除 FAQ/声明等）；文末加上一篇/下一篇；样式内联注入文章页。浏览器验证通过。

---

### P0-5 深页面"退出通道"少：footer 过简、移动导航难用 —— ✅ footer 完成（2026-08-09）

**用户现象**
- 工具 / 文章页 footer 只有版权和备案号，没有"关于 / 联系 / 隐私 / 收藏 / 投稿"（这些只在首页 footer）；
- 移动端全局导航 11 项横向滚动，"我的收藏"要划很久才找得到。

**涉及位置**
- 全站 footer 模板（首页 `index.html` 有完整链接，内页缺失）；
- 全局导航（`index.html` 及内页）。

**建议方案**
1. 全站统一 footer（关于、联系、隐私、收藏、投稿）；
2. 移动端导航收敛为汉堡菜单 / "更多"折叠；
3. 顶部增加"收藏"快捷图标。

**预估**：S（模板统一 + 少量样式）

**完成情况**：全站后处理为内页 footer 统一注入"关于/联系/收藏/隐私/友情/投稿"链接（含 404）。移动端导航折叠（汉堡菜单）暂缓，现有横向滚动 + 收藏 FAB 可用，列入 P1 后续。

---

## P1 - 性价比高的便利性功能

### P1-1 收藏支持导出 / 导入 —— ✅ 已完成（2026-08-09）
- 现象：收藏存于 LocalStorage，换设备 / 清缓存即丢失；`favorites.html` 目前只有"清空收藏"。
- 建议：增加"导出 JSON / 导入 JSON / 复制收藏清单"按钮，让用户可以带走和分享。
- 完成：favorites.html 增加导出/导入/复制清单按钮；favorites.js 实现 JSON 下载、文件导入合并去重、剪贴板复制。

### P1-2 工具页增加"信息有误？"纠错入口 —— ✅ 已完成（2026-08-09）
- 现象：价格、免费额度、官网链接变化很快，用户发现错误时没有顺手反馈的通道。
- 建议：每个工具页 CTA 区加"报错 / 纠错"小按钮（预填工具名，跳联系页或 mailto）。
- 完成：工具页 action-bar 增加"信息有误？"，跳转 contact.html?tool=slug；联系页读取参数显示反馈面板并预填邮件主题。

### P1-3 快讯 / 文章页放可见的 RSS 订阅按钮 —— ✅ 已完成（2026-08-09）
- 现象：`rss.xml` 已生成，但入口只在首页 `<head>` 中，普通用户看不到。
- 建议：快讯页和文章列表页增加可见的 RSS 订阅按钮。
- 完成：文章列表页、文章分类页、快讯页（含日期/分类子页）均加入 RSS 订阅按钮；
  2026-08-10 修复移动端布局：分类页 RSS 胶囊改为与类目名称同行（名称在左、订阅在右），
  不再独占一行挤在"类目介绍"与"更多栏目"之间（320/390/768/1280 视口实测通过）。

### P1-4 文章列表分页增加页码 —— ✅ 已完成（2026-08-09）
- 现象：`articles/index.html` 只显示"1 / 17"和"下一页"，跳到第 10 页需连点 9 次。
- 建议：增加数字页码（当前页高亮、首页 / 末页直达），移动端可跳页输入。
- 完成：新增 `_pagination_html`（当前页 ±2 + 首尾页 + 省略号），文章列表与分类分页统一使用；页码样式已入 style.css 并重生成 min/critical CSS。

### P1-5 移动端"添加到主屏幕"（PWA 基础） —— ✅ 已完成（2026-08-09，SW 于 2026-08-10 补齐）
- 现象：站点无 manifest / 图标，无法像 App 一样放到桌面。
- 建议：补 `manifest.json`、`apple-touch-icon`、`theme-color`；Service Worker 可选（导航类站点离线缓存价值高）。
- 完成：新增 manifest.json（2026-08-13 从 manifest.webmanifest 改名，nginx 默认对 .webmanifest 返回
  application/octet-stream 会导致部分浏览器判不可安装）+ 192/512 PNG 图标，全站注入 manifest/theme-color/apple-touch-icon；
  2026-08-10 补充 Service Worker（sw.js，网络优先+离线兜底）并全站注册——Chrome 的 PWA 安装条件要求 SW，
  否则"安装"后桌面不出独立图标。离线缓存策略已就位。

### P1-6 分享 / 复制链接 —— ✅ 已完成（2026-08-09）
- 现象：工具卡片和文章没有复制链接 / 分享入口，对比结果也无法一键分享。
- 建议：工具详情页加"复制链接"，对比 / 排行页加"分享结果"。
- 完成：工具详情页 action-bar 增加"复制链接"按钮（favorites.js 通用 `[data-copy-link]` 逻辑，后续对比/排行页可直接复用）。

---

## P2 - 需要产品 / 后端决策的长期项（与 ROADMAP-TODO.md 对齐）

- 用户账号体系：收藏、点赞跨设备同步（需引入 BaaS，待拍板）；
- 评论 / 口碑社区：评估 Disqus / Waline 等无后端方案；
- 排行升级：从"编辑评分"走向"用户投票 / 使用数据"驱动；
- 更新订阅 / 提醒：工具价格、版本变化主动通知用户；
- 数据驱动迭代：按 GSC 点击率、搜索词持续优化标题与内容。

---

## 已完成 / 保留的好体验（不要回退）

- 首页搜索 + 热门词快捷入口（点击后自动滚动到结果区）；
- 收藏 FAB、暗色模式、正文朗读（TTS）、AI 工具助手；
- 面包屑导航、相关工具 / 文章推荐、竞品对比与替代方案；
- 外链统一新窗口 + `rel="nofollow noopener"`；
- 404 页设计（搜索 + 推荐入口，修复 P0-1 后即完整可用）；
- 表格在移动端自适应（word-break + 横向滚动容器）。

### 2026-08-13 追加：暗色模式"白底灰白字"漏网兜底 —— ✅ 已完成（2026-08-13）
- 现象：暗色模式下 Quiz 答题卡、排行页前三名卡/深度解读区、CPS 推广卡、
  个别文章正文的内联 `<pre>`/表格行/延伸阅读段出现"浅色底 + 浅灰字"看不清。
- 根因：2026-07-15 暗色修复是白名单式枚举，上述组件/写法不在白名单内；
  CPS 卡（2026-08-12 新增）更是完全没有暗黑分支；暗色"文字提亮"为全局
  `!important`，遇到未转深的浅底反而放大问题。
- 完成：Quiz/排行模板改 CSS 变量；loader.js 补 CPS 卡暗黑分支；
  style.css 新增第 17 节（正文内联浅底兜底 + TL;DR 文字提亮 + 各组件暗色兜底）；
  新增 `scripts/check_dark_mode.py` 守卫并挂入 deploy.sh（build 后自动校验）。
- 验收：Quiz/排行/资讯/文章四类页面 × 320/390/768/1280 视口暗色低对比扫描全部为 0；
  浅色模式外观无回归（Quiz 答题卡白底深字、CPS 卡白底深字、内联 pre 浅底深字）。

### 2026-08-31 追加：第二轮走查修复（agent-browser 实测 桌面+390px + 线上验收）

| # | 问题 | 根因/位置 | 状态 |
|---|------|-----------|------|
| 1 | CPS 推广卡插在"标题与其首段"之间切断阅读（工具页+文章页移动端） | `ads/loader.js` insertAfterNthP 找第 N 个 `<p>` 前插入，该 `<p>` 恰在 h2/h3 后即命中 | ✅ 已修（顺延到同段落块下一 `<p>`；被 ul/table 挡住时插到标题之前）|
| 2 | 移动端 FAB 遮挡「信息有误？」 | 悬浮球与 CTA 区重叠 | ✅ 用户复核判定不算问题（左侧点击空间充足），不改 |
| 3 | 作者卡工具数硬编码"580+"（198 篇文章页一致旧数） | `render_article.py:693` | ✅ 已修（改 `{build.TOOL_COUNT}` 动态值，线上验证 627+）|
| 4 | /dict/ 顶部导航与主站不一致（缺首页/AI动态/词典入口） | `render_news_dict.py` 两处自有精简导航 | ✅ 已修（统一用 `build.GLOBAL_NAV`）|
| 5 | 工具页/列表卡"月访问N/A"外露（19 个工具 visits='N/A'） | `render_tool.py:594/178` + `render_index.py:96` 过滤列表不含 N/A | ✅ 已修（无数据值统一不渲染）|
| 6 | 移动端 11 项横向滚动导航（P0-5 历史残留） | 全站导航收口 `build.py:GLOBAL_NAV` | ✅ 已修（移动端 ☰ 折叠按钮，展开为换行网格；桌面不变；favorites.html/404.html 静态文件已同步）|
| 7 | 移动端搜索页疑似双层 header 叠影 | — | ✅ 排除（header 不透明无 backdrop-filter；本地+线上多滚动位置复现失败，判定为截图时序伪影）|

性能复核：gzip 正常（tools 页 878KB→传输 96KB）；tools-data.js 454KB（较 8/09 +24%，随工具数增长，>600KB 再考虑分类懒加载）。
验收：本地 6 页抽查 CPS 卡前元素均非标题；线上 390px 汉堡展开 11 项全可见；deploy 健康检查 1167 URL 全活；commit 411f123 + ab0270b。
