# 2026-05-13

## aitoolbox 每日维护（自动化任务）
- 发布 3 个新工具：灵办AI、Devin AI、Ironclad AI
- 发现并修复 build.py bug：23个工具 badge=null 导致首页构建崩溃（t.get('badge', {}) 不能处理 badge=null）
- 修复方案：tools.json 修复数据（badge=null→{}）+ build.py 改用 `(t.get('badge') or {})` 
- 构建成功、阿里云增量部署成功（17个变化文件）、Git push 首次失败重试后成功（commit cfa8546b）
- 库存：195已发布/39未发布/总计231（总工具数从197->231）
