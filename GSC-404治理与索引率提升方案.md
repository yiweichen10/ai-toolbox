# GSC 索引问题治理方案（2026-08-27）

> 数据来源：GSC 覆盖导出 7 个文件（2026-08-27）+ 线上 sitemap 全量 HEAD 实测（1089 条，0 条失效）+ 本地构建产物比对。

## 0. 关键实测结论（方案前提）

- **线上 sitemap 1089 条 URL 全部返回 200**，sitemap 本身没有死链。
- 404 的 149 条中 **145 条不在当前 sitemap**——它们是历史删页/改名留下的"旧地址"，Google 靠缓存和旧索引清单反复回访产生。
- 404 里 **33 条确认有新页替代**（改名/换 slug），已逐条与线上 sitemap 精确核对，映射见 `gsc-404-redirects-v1.csv`。
- 8/22 曾出现 4 个页面（readwise/pydantic-ai/google-adk/pixverse）抓取时 404、现已恢复 200——说明存在一次部署不完整窗口，属流程风险，非当前遗留问题。

## 1. 问题定性（哪些要修、哪些不用管）

| 问题 | 数量 | 定性 | 处置 |
|------|------|------|------|
| 404 | 149 | 33 条有替代页需 301；116 条为彻底删除的旧页 | 加 nginx 301（档A/B）；其余保持 404 |
| Discovered-not-indexed | 262 | 抓取预算+内容同质 | 提升内容独占性，见 §3 |
| Crawled-not-indexed | 42 | 同上，Google 已抓但判质量不足 | 见 §3 |
| Alternate+canonical | 44 | canonical 正常工作，非故障 | 不动；可选在 robots 屏蔽 contact.html?tool= |
| noindex | 27 | 有意 noindex 的分页/收藏页 | 不动（"符合预期"） |
| 重定向 | 3 | 尾斜杠变体 | 确认 nginx 斜杠规范化即可 |
| Duplicate | 1 | /quiz// 双斜杠 | 修模板拼接 bug |

## 2. 404 治理方案（回答问题 1：有替代→跳转，无替代→正确 404）

### 档 A：改名/换 slug，有真实替代页 → 301（22 条，最高优先）
例：`/tools/notion/` → `/tools/notion-ai/`、`/category/writing/` → `/category/ai-writing/`、`/compare/cursor-windsurf/` → `/compare/cursor-vs-windsurf/`。
301 能把旧页残余权重转移给新页，并让 Google 尽快从 404 清单中移除。

### 档 B：工具已从目录下架，无精确替代 → 301 到 `/tools/` 索引页（11 条）
百度翻译、有道、钉钉等下架工具页。跳到分类索引是 Google 接受的"软替代"做法，好过死 404。

### 档 C：无替代（周报文章、旧版对比页等 ~116 条）→ 保持 404，不做假跳转
- 跳到不相关内容会触发"软 404"判定，比干净 404 更伤。
- 404 页确保返回真实 404 状态码 + 提供搜索框/热门分类链接（现有 404.html 已具备即可）。
- 预期：Google 回访 2-3 次确认 404 后逐步放弃，清单会在 4-8 周内自然缩小。

### 执行方式
1. 追加到 `nginx-old-url-redirects.conf`（已有同类机制，8/13 Bing 修复时建的），格式 `location = /旧路径/ { return 301 全URL; }`。
2. `nginx -t && nginx -s reload`，抽查 301 生效。
3. GSC「验证修复」+ 对档 A 目标页提交抓取（可加速回收）。
4. （流程补强）deploy.sh 部署完成后自动 curl 抽查 5 个关键页 200，防再发 8/22 式部署缺口。

## 3. 索引率提升（528/1089 ≈ 48% → 目标 65%+）

Google 不收录的 304 条集中在 tools(164+20) / articles(62+6) / dict(9+8)，模板同质是主因。按投入产出排序：

1. **停掉"宽度优先"扩张，先消化存量**：sitemap 已 1089 条，新工具/新文章上线速度暂缓，让抓取预算集中。
2. **工具页差异化**：对 164 个 Discovered 工具页分批补"真实使用数据/价格截图/独有评测段落"（沿用每批 5 个的节奏），有独占信息的页 Google 才愿意收。
3. **内链权重再分配**：首页/分类页给"未收录候选页"更多入口（今日推荐、相关推荐位优先放 Discovered 清单里的页面），提升其 PageRank 信号。
4. **低价值页主动收缩**：对长期不收录且内容单薄的页面（老周报文章、批量词典短词条），考虑合并进列表页并 301——sitemap 从 1089 缩到 800 以内、收录率反而上升，是常见且健康的结果。
5. **新闻类页面降级**：`/news/日期/` 日更页天然易被判"聚合内容"，不追收录，保住 articles 长文即可。

## 4. 小修（顺手做）

- `/quiz//`：build.py 模板中 quiz 链接拼接多余斜杠，修一处拼接逻辑，全站回归时验证。
- 3 条 redirect 记录：确认 nginx `try_files`/斜杠规范化配置统一 `/a` → `/a/` 单向 301。
- contact.html?tool=… 变体增长：若继续膨胀，robots.txt 加 `Disallow: /contact.html?`（当前 canonical 已兜底，不急）。

## 5. 时间表与验证指标

| 周 | 动作 | 指标 |
|----|------|------|
| 本周 | 档A+B 301 上线（33 条）；quiz// 修复 | 404 清单开始回落 |
| 2-4 周 | 工具页差异化第 1-2 批（每批5个）；deploy 抽查补进 deploy.sh | Discovered <200 |
| 4-8 周 | 低价值页合并收缩第一轮；GSC 复查导出 | 总收录 528 → 600+，索引率 55%+ |

**每两周从 GSC 重导一次覆盖数据对比趋势，指标以"已收录数"和"404 存量"为准，不看问题条数（多数是记账噪音）。**
